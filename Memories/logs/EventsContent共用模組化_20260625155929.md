# EventsContent共用模組化

時間：2026-06-25 15:59:29

## 任務範圍

- 檢查 `SmartExpoIoT/Views/EventsAdmin/Content.cshtml` 與 `SmartExpoIoT/Views/Events/Content.cshtml` 的重複 Razor/Vue markup。
- 參考 `Views/Shared/vue-component/HeaderFunction.cshtml` 的 `text/template` + Vue component 模式，抽出共用模組。

## 主要變更

- 新增 `SmartExpoIoT/Views/Shared/vue-component/EventContent.cshtml`：
  - 定義 `event-content-template` 與 `eventContentComponent`。
  - 共用 tab nav、基本設定、活動報名、獎項設定、電子票券、識別證列印、前台登入客製 UI。
  - 將獎項設定 inline style 收斂為 `.award-*` 與 `.lottery-qr-dialog` class。
  - 用 props 控制差異：`lockOnEdit`、`lotteryModeName`、`showLotteryQrButton`、`disableAllowSelfQuery`。
- 簡化 `Events/Content.cshtml`：
  - 引入 shared component。
  - 保留 Events 編輯時鎖欄位規則。
- 簡化 `EventsAdmin/Content.cshtml`：
  - 引入 shared component。
  - 保留客戶/報價欄位、公司清單載入、手機 QR 綁定 modal 與相關方法。

## 驗證結果

- `dotnet build SmartExpoIoT/SmartExpoIoT.csproj` 成功，0 errors；warnings 為既有 NuGet、nullable、obsolete、未使用變數等。
- 靜態搜尋確認三個相關檔案沒有 `style=` inline attribute。
- 臨時啟動 `https://localhost:5001` 後用 Playwright 登入並檢查：
  - `EventsAdmin/Create` 正常 render，看到基本設定與進階選項，`event-content` custom element 已被 Vue 取代。
  - `Events/Create` 以目前測試帳號回 `權限不足`，無法做該 route 的瀏覽器驗證；此頁已由 Razor build 覆蓋。
- 驗證後已停止臨時站台並移除啟動 log。

## 注意事項

- 工作樹原本已有 `SmartExpoIoT/Controllers/EventAwardController.cs` 與 `SmartExpoIoT/Views/EventAward/Content.cshtml` 修改，本次未處理、未回復。
