# EventContent樣式抽離

時間：2026-06-25 16:26:41

## 任務範圍

- 將 `SmartExpoIoT/Views/Shared/vue-component/EventContent.cshtml` 頂端的 `<style>` 樣式抽離成 CSS。
- 保留既有 Vue template、props、computed 與 methods 行為不變。

## 主要變更

- 移除 `EventContent.cshtml` 內的 `<style>` 區塊。
- 將 `.nav-link-tab`、`.award-*`、`.lottery-qr-dialog` 樣式搬到 `SmartExpoIoT/wwwroot/css/site.css`。
- 沿用 `_ScriptsAndCss.cshtml` 既有 `~/css/site.css` 全站載入方式，未新增額外 CSS link。

## 驗證結果

- `rg` 確認 `EventContent.cshtml` 沒有殘留 `<style>`、`</style>` 或 `style=`。
- `dotnet build SmartExpoIoT/SmartExpoIoT.csproj` 成功，0 errors；warnings 為既有 NuGet 相容性、nullable、obsolete、未使用變數等。

## 注意事項

- 工作樹原本已有其他修改與未追蹤的 `EventContent.cshtml`；本次未回復、未整理那些既有變更。
